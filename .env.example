# ─────────────────────────────────────────────────────────────
# Contact form email (SMTP)  —  copy this file to .env.local and
# fill in the values. NEVER commit .env.local (it is gitignored).
# ─────────────────────────────────────────────────────────────

# Gmail / Google Workspace SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# The mailbox that authenticates AND sends the message.
SMTP_USER=andrey@lumintik.com

# Google "App Password" (16 chars, no spaces) for the account above.
# Generate at: Google Account → Security → 2-Step Verification → App passwords.
# Paste it here WITHOUT spaces, e.g. SMTP_PASSWORD=abcdabcdabcdabcd
SMTP_PASSWORD=

# Where contact submissions are delivered (defaults to SMTP_USER if unset).
CONTACT_TO=andrey@lumintik.com,david@lumintik.com

# ─────────────────────────────────────────────────────────────
# Analytics (PostHog)  —  optional. Leave the key empty to
# disable tracking entirely; the site works without it.
# ─────────────────────────────────────────────────────────────

# Project API key — Project settings → Project ID / API key.
# Starts with "phc_". This one is public by design (it ships to the browser);
# a "phx_" key is a personal API key and must never be used here.
NEXT_PUBLIC_POSTHOG_KEY=

# There is no host variable: analytics is served from this domain at /ingest
# and rewritten to PostHog in next.config.ts, so ad blockers can't drop it.
# The project region is set by those rewrite destinations (currently US).
